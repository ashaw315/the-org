# the-org — the machinery
ANTHROPIC_API_KEY="sk-ant-..."
DATABASE_URL="postgres://...neon..."   # the SAME database the-product reads
# optional override; defaults to a Sonnet-class model
# CLAUDE_MODEL="claude-sonnet-4-6"
